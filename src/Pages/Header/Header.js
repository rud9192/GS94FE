import React from 'react';
import Header_Navigator from './Header_Navigator'

const Header = () => {
    return (
        <div>
            <Header_Navigator />
        </div>
    );
};

export default Header;